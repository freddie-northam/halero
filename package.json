{
  "name": "halero",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "modules/*",
    "connectors/*"
  ],
  "scripts": {
    "test": "bun test",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "typecheck": "bun run --filter '*' typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.2",
    "@types/bun": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
