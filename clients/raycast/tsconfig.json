{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Raycast view commands use the automatic JSX transform (per the official extension template).
    "jsx": "react-jsx"
  },
  // raycast-env.d.ts is generated by `ray develop`; harmless when absent.
  "include": ["src", "raycast-env.d.ts"]
}
