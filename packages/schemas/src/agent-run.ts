// The agent.run entity kind: a Developer-page agent run recorded on the
// spine so it is searchable, linkable, and timeline-able like any other
// object. Spine-first (title = the prompt); the durable satellite (agent,
// branch, status, diff stats) follows in a later increment.
export const AGENT_RUN_KIND = "agent.run";
