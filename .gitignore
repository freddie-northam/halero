# dependencies
node_modules/

# build outputs
dist/
build/
# regenerated by ray develop/build
clients/raycast/raycast-env.d.ts

# environment
.env
.env.*
!.env.example

# runtime data
data/
*.db
*.db-wal
*.db-shm

# scratch
.superpowers/

# agent worktrees for concurrent sessions (each carries its own checkout,
# including its own biome.json, which trips biome's nested-root-config
# check when run from the main checkout)
.claude/worktrees/

# editor
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.swp
.gstack/
